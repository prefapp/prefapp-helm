{{- define "job.test.data" -}}

name: my-job

backoffLimit: 4

failedJobHistoryLimit: 4

pod_labels:
  foo: 1
  faa: 2

containers:
  - name: job 
    image: {{ .Values.image }}
    command:
      - cat
      - /etc/file.txt


{{- end -}}

{{ include "ph.job.render" (include "job.test.data" . | fromYaml ) }}
